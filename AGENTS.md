# Gamo 공동 작업 규칙

이 저장소는 여러 Godot 게임을 한곳에서 관리한다. 각 게임은 루트 바로 아래의 독립 폴더이며, `project.godot`와 Web export preset을 포함해야 한다.

이 파일이 공동 작업 규칙의 단일 원본이다. Codex는 이 파일을, Claude Code는 `CLAUDE.md`를 자동으로 읽으며 `CLAUDE.md`는 `@AGENTS.md` import로 이 내용을 그대로 가져온다. 규칙을 바꿀 때는 이 파일만 편집하고 `CLAUDE.md`에 본문을 복사하지 않는다.

## 작업 원칙

- 작업을 시작하기 전에 이 파일과 루트의 `Progress.md`를 확인한다.
- Motorio의 게임 정체성·레벨 디자인·자동화 디자인은 `motorio/design/`의 세 문서가 단일 원본이다. 기능을 만들기 전에 해당 문서를 먼저 읽고, 문서와 다르게 구현하지 않는다. 설계가 바뀌면 코드보다 문서를 먼저 고친다.
- 설계 문서의 각 항목은 `[확정]`·`[초안]`·`[질문]` 상태를 갖는다. `[질문]`으로 표시된 항목은 사용자 결정 없이 임의로 구현하지 않는다.
- 설계 문서는 `https://7bvcxz.github.io/gamo/design/`에서 조회한다. 이 뷰어는 Pages CDN 캐시를 피해 GitHub 저장소 API로 원문을 직접 읽으므로, 문서를 push하면 새로고침만으로 반영된다.
- 기능 작업이 끝날 때마다 `Progress.md`에 완료 내용, 검증 방법, 다음 작업을 갱신한다.
- 작업 중 실제로 발생한 실수나 재발 가능성이 있는 함정을 발견하면 아래 "실수와 교훈"에 원인과 예방책을 추가한다.
- 기존 사용자의 변경은 덮어쓰거나 되돌리지 않는다.
- Godot 프로젝트는 가능하면 외부 에셋 없이도 처음 clone한 상태에서 실행되어야 한다.
- 완료 전 `godot --headless --path <게임 폴더> --editor --quit`로 로드 오류를 확인하고, 가능하면 Web export도 검증한다.
- Web 배포 로더나 runner를 변경하면 정적 검사만으로 완료하지 않는다. 실제 브라우저에서 공개 또는 로컬 HTTP URL을 열어 Godot 캔버스가 초기화되고 오류 화면이 없으며 게임 프레임이 실행되는지 자동 브라우저 테스트로 확인한다.
- 기능 작업은 관련 자동 테스트와 실제 실행 검증이 모두 통과하기 전에는 완료로 보고하거나 push하지 않는다. 실행 환경을 준비할 수 없다면 완료라고 하지 말고 차단 상태와 필요한 검증을 보고한다.
- 새 게임을 추가하면 Web 배포 후 HeyDive 관리자 화면/API에도 게임명, 설명, 태그, `Release` 상태, 배포된 절대 `embedUrl`을 등록하고 실제 목록 노출과 실행을 확인한다.
- HeyDive 등록에 인증이나 서버 접근 권한이 필요해 자동 완료할 수 없다면, 정적 배포 완료와 HeyDive 등록 미완료를 명확히 구분해 보고하고 사용자에게 필요한 최소 작업을 요청한다.
- 완료된 변경은 관련 파일만 커밋하고 `main` 브랜치를 원격에 push한다.
- `motorio`를 push하기 직전에 `motorio/project.godot`의 `application/config/version` patch 값을 `0.0.1` 올리고, 게임 우측 아래 표시와 Web export가 같은 버전을 사용하는지 확인한다.
- `motorio-oneshot`도 같은 규칙을 따른다. 개발이 한 번 끝나 push하기 직전에 `motorio-oneshot/project.godot`의 `application/config/version` patch 값을 `0.0.1` 올리고, 타이틀 화면 우측 아래 표시가 같은 버전인지 확인한다. `0.5.0`이 최초로 기록된 버전이다.
- minor 또는 major 버전은 사용자가 명시적으로 요청할 때만 올린다. 사용자 요청 없이 patch 증가를 minor/major 증가로 대체하지 않는다.
- Motorio 배포 시 `motorio/release.md`에 해당 버전과 핵심 변경 사항을 간략히 추가한다.
- `motorio-oneshot`의 변경 사항은 `web/components/content/OneShotReleases.jsx`에 버전별로 추가하고, 끝난 작업은 `OneShotTodo.jsx`에서 지운다. 두 문서는 `https://7bvcxz.github.io/gamo/doc/`의 MOTORIO: ONE SHOT 섹션에서 조회한다.

## 배포

- `./deploy-web.sh <게임명>`은 게임을 `docs/<게임명>/`에 Web export한다.
- 저장소 웹사이트는 `web/`의 Next.js(App Router, 정적 export) 소스다. 서버 코드는 없으며 `output: 'export'`라 Vercel·GitHub Pages·`deploy/`의 nginx 어디서든 같은 파일이 동작한다.
- 사이트 빌드는 대상이 둘이다. `npm run build`는 base가 `/`인 Vercel용이고, `npm run build:pages`는 base가 `/gamo`인 Pages용으로 `docs/`에 넣는다. 게임과 사이트는 호스트가 갈렸다 — 게임 150MB는 Vercel Hobby의 100MB 업로드 한도를 넘어 Pages에 남고, 사이트는 초 단위로 재배포되는 쪽으로 간다.
- 경로를 소스에 통째로 적지 않는다. 사이트 내부 링크는 `site()`, 게임 링크는 `game()`을 쓴다(`web/lib/links.js`). 호스트가 갈렸기 때문에 이 둘은 서로 다른 값이 된다.
- 스프라이트 후보는 `web/public/sprite-candidates/`에 발행된다. 사이트와 함께 배포되는 자주 바뀌는 콘텐츠이기 때문이며, 게임 바이너리가 `docs/`에 남는 것과 정확히 반대 이유다.
- `web/scripts/to-docs.mjs`는 `docs/` 안에서 `motorio`·`motorio-oneshot`·`nowhere`만 남기고 사이트 파일을 교체한다. 이 목록을 지우거나 디렉터리를 통째로 비우면 게임 Web export 150MB가 사라진다. (Vite 시절 `emptyOutDir: false`가 막던 것과 같은 함정이다.)
- GitHub Pages는 `main` 브랜치의 `docs/`를 서비스한다.
- 새 게임을 추가할 때 `export_presets.cfg`의 Web preset을 반드시 포함한다.
- 새 게임의 배포 URL은 `https://7bvcxz.github.io/gamo/<게임명>/index.html` 형식이며, 이 절대 URL을 HeyDive의 `embedUrl`로 사용한다.
- Web export 후 `deploy-web.sh`가 엔진/PCK/게임 HTML에 내용 해시를 붙인다. 고정 `index.html`에는 배포 시점의 PCK 이름·크기가 직접 기록되고, 추가 요청 없이 즉시 runner로 넘긴다. runner는 PCK를 Pages → raw.githubusercontent → 같은 디렉터리의 `version.json` → 자기 빌드의 mainPack 순으로 해결한다. 생성된 해시 파일명, manifest, 로더, runner를 수동으로 되돌리지 않는다.
- **게임 로딩 경로에 `api.github.com`을 쓰지 않는다.** 비인증 GitHub API는 IP당 시간당 60회이고, 모바일 캐리어 NAT처럼 IP를 공유하면 쉽게 소진되며, 소진되면 403이라 게임이 아예 열리지 않는다. Pages CDN의 10분 캐시로 인한 지연은 플레이어에게 보이지 않지만, 열리지 않는 게임은 보인다. 최신 빌드를 즉시 확인해야 하면 API가 아니라 해시가 붙은 `game-*.html`을 직접 연다.
- runner는 쿼리스트링을 상수로 읽은 직후 `history.replaceState`로 주소를 게임 디렉터리(`/gamo/<게임명>/`)로 되돌린다. 플레이어와 HeyDive `embedUrl`이 항상 같은 주소를 쓰게 하기 위한 것이며, index와 runner가 같은 디렉터리에 있으므로 상대 경로 해석은 바뀌지 않는다. runner 위치를 다른 디렉터리로 옮기면 이 처리를 함께 재검토한다.

## Motorio 블록 특성 용어

- `Solid`: 주인공 및 다른 Solid 블록과 서로 겹칠 수 없는 고체 블록. `solid` 그룹과 Solid 공용 충돌 레이어를 사용한다. 일반 상자, 고양이 블록, 미네랄 자원이 해당한다.
- `Machine`: 주인공과는 겹칠 수 없지만 Solid를 포함한 다른 블록과는 겹칠 수 있는 기계 블록. `machine` 그룹과 Machine 공용 충돌 레이어를 사용한다. 현재 해당 블록은 없다.
- `TransportFloor`: 주인공과 모든 블록이 위에 겹칠 수 있고 운송 효과만 주는 최하단 바닥 블록. `transport_floor` 그룹을 사용하며 컨베이어 벨트가 해당한다.
- `Fixed`: Z로 회수할 수 없고 TransportFloor를 제외한 주인공, Solid, Machine, 다른 Fixed와 겹칠 수 없는 고정 블록. `fixed` 그룹과 Fixed 공용 충돌 레이어를 사용한다. 미네랄 원석 블록이 해당한다.
- 일반 블록을 X로 설치해도 별도 특성이나 고정 상태를 부여하지 않고 해당 블록의 원래 이동성을 유지한다.
- 기둥 블록은 밀리지 않는 freeze 상태의 `Solid`이지만 Z로 회수하고 X로 다시 설치할 수 있으므로 `Fixed`는 아니다.
- 박스 생성기는 방향을 가진 2×1 freeze `Solid`이며 Z 회수와 X 회전 설치가 가능하다. 뒤 입력에서 미네랄 자원 3개를 소비해 앞 출력으로 일반 상자 1개를 만들고, 출력이 막히면 생산분을 보존한다.
- 제련소는 2×1 freeze `Solid`이며 Z 회수와 X 회전 설치가 가능하다. 뒤 입력에서 미네랄 2개와 석탄 1개를 받아 2초 뒤 앞 출력으로 금속판 1개를 만들고, 출력이 막히면 생산분을 보존한다. 금속판은 운송·기지 집계가 되는 `world_resource`이며 기지 7단계 업그레이드에 물고기와 함께 필요하다. 기지 5단계에서 해금한다.
- 화면 밖 기계는 시뮬레이션은 계속하되 `WorldView.is_on_screen()`으로 `queue_redraw()`만 건너뛴다. 매 프레임 다시 그리는 기계를 새로 추가할 때도 같은 검사를 넣는다.
- 분배기는 모든 물체와 겹칠 수 있는 `TransportFloor`이며 들어온 Solid를 진행 방향 기준 좌우로 번갈아 보낸다. 첫 자동화 목표 완료 후 기지 레벨 2에서 해금된다.
- 구리·석탄·수정·석유·우라늄 원석은 거리별로 배치되는 `Fixed`이며 각각 기본 광부, 먹은 광부, 전력, 압력 고양이, 전력+석유 조건을 사용한다. 채굴 결과는 `world_resource` Solid로 운송·기지 집계한다.
- 물 타일은 통행 불가 `Fixed`이고 다리는 해당 타일의 충돌만 해제하는 `TransportFloor`이다. 다리 회수 시 반드시 원래 물 충돌을 복원한다.
- 고양이 작업자는 일할 때 hunger가 감소하며 낮은 hunger에서는 작업 주기가 느려진다. 요리 고양이+치즈 밭이 치즈를 만들고 서빙 고양이가 주변 작업자를 먹인다.
- 하루는 빠른 시작 완료 후 흐르는 12분이며 11분부터 고양이가 작업을 멈추고 귀가한다. 플레이어는 기지 남서 `숙소`에서 모닥불 취침하며, 12분 도달 시 고양이가 구조한다. 체온이 0이어도 강제 귀환시키지 않는다.
- 전력·식량 생산은 각각 발전기×전기 고양이, 낚시장×낚시 고양이의 작은 쪽 개수만큼 확장한다. 서빙 고양이는 물고기를 소비해 작업자를 먹인다. 추가 고양이만으로 장비 용량을 초과하지 않으며 HEAT/POWER/FOOD 기술은 반복할수록 비용과 효과가 증가한다.
- 연구소는 기지 3단계에서 제작하는 회수·재설치 가능한 3×3 freeze `Solid`이다. 설치한 연구소를 바라보고 Z로 열며, 열·전력·식량 연구는 각각 최초 10·20·30분이 걸리고 반복 단계마다 10분씩 늘어난다. 전역 연구는 한 번에 하나만 진행하며 메뉴를 닫아도 시간이 흐른다.
- Motorio 저장은 Web `user://motorio_save.cfg`에 30초마다 자동 기록하고 수동 저장 버튼도 같은 스키마를 사용한다. 플레이어·인벤토리·자원·진행·생존·연구와 회수 가능한 월드 오브젝트를 함께 복원하며, 스키마 버전이 다르면 안전하게 새 게임으로 시작한다.
- 기지는 1~7단계이며 업그레이드마다 온기 반경이 3칸 증가한다. 상자→미네랄→구리→물고기 순으로 희귀도가 높아지는 비용을 요구하고, 제작법은 기지 단계에 따라 순차 해금한다.
- 모든 자원은 월드 생성 시점에 고정 좌표로 배치한다. 기지 레벨은 절대 자원을 새로 생성하지 않고 온기 반경(`8 + 3×레벨`)만 넓혀서 이미 존재하는 자원 지대에 도달할 수 있게 만든다. 미네랄 씨앗 지대는 반경 10·12·14·17·20·24·28·32에, 단계 원석 고리는 구리 19·석탄 22·수정 25·석유 27·우라늄 29에 둔다.
- 온기 반경 밖은 플레이어 위치와 무관하게 흰 월드 안개로 가리되, 반경 바로 바깥 9칸은 옅게 유지해 다음 자원 지대가 보이게 한다. 안개와 냉기 화면 효과는 위험을 알리는 용도이며 플레이어의 시야를 가려 탐사를 불가능하게 만들지 않는다. 체온 0에서는 이동과 Z/X를 잠그고 3초 후 기지 남쪽에서 체온 60으로 구조한다.
- 모바일 조이스틱 이동은 45도 단위 8방향이며, 기지 제작소가 열렸을 때는 이동 대신 위·아래로 제작 항목을 선택한다.
- 기지 투입구 칸에도 X 설치를 허용한다. 컨베이어·분배기 같은 `TransportFloor`는 다른 블록이나 자원이 있는 칸에도 겹쳐 설치할 수 있어야 한다.
- UI는 빠른 시작→기본 자동화→외부 탐사→전력 시대→후반 산업 단계로 공개하며, 모바일에서 생존 정보·목표·터치 조작이 서로 겹치지 않는지 실제 Web 화면으로 확인한다.
- BOX, MINERAL 및 앞으로 추가할 수집 자원 수치는 좌측 상단에 모아서 표시한다.
- 기지의 상자 투입구는 Solid 전체가 아니라 `box_block` 그룹의 일반 상자만 회수한다.

## 자체 호스팅

- `deploy/`의 nginx 컨테이너가 이 머신에서 `docs/` 빌드를 서빙한다. `docker compose -f deploy/docker-compose.yml up -d`로 띄우고 배포는 `./deploy/publish.sh`이며, 초 단위로 끝나고 반환 시점에 이미 live다. GitHub Pages는 그대로 두며 둘은 같은 파일을 서빙한다.
- 서빙 루트는 저장소의 `docs/`가 아니라 `~/srv/gamo`다. 작업 트리를 직접 마운트하면 빌드 중간 상태가 공개되고 `.gz`가 소스에 흩어진다.
- 공개 호스트명은 Cloudflare 대시보드에 있다(터널이 토큰 방식이라 저장소에 넣을 수 없다). 자세한 절차는 `deploy/README.md`.
- 사이트는 `/gamo/` base를 유지한다. 두 호스트가 바이트 단위로 같은 파일을 주고, 어느 쪽이든 재빌드 없이 끌 수 있다.

## 실수와 교훈

- 2026-07-22: `godot --headless`의 더미 렌더러에서도 Viewport texture를 PNG로 저장할 수 있다고 가정해 방향별 고양이 캡처가 null texture로 실패했다. 실제 렌더 이미지 검증은 WebGL 브라우저나 렌더링 드라이버가 있는 실행 환경에서 수행하고, headless는 파싱·로직 테스트에만 사용한다.
- 2026-07-22: 시각 개선 중 Godot `Rect2`에 존재하지 않는 `translated()` 메서드를 사용해 상자 스크립트가 파싱되지 않았다. 좌표 이동 사각형은 `Rect2(original.position + offset, original.size)`로 명시하고, 새 API 사용 직후 편집기 로드로 확인한다.
- 2026-07-22: 여러 중간 Web 해시 파일을 한 번의 `unlink` 호출로 정리하려 해 `extra operand`로 실패했다. `unlink`는 정확한 단일 경로마다 별도로 호출하고, 여러 파일을 정리할 때도 대상 목록을 먼저 확인한 뒤 각 명령을 명시적으로 실행한다.
- 2026-07-22: 모바일 전역 터치 핸들러 안에서 초기화와 씬 reload를 즉시 실행해, 사라진 Viewport에 뒤이어 `set_input_as_handled()`가 호출되는 런타임 오류가 발생했다. 터치로 씬을 교체하거나 트리를 비우는 동작은 `call_deferred()`로 현재 입력 이벤트 처리가 끝난 다음 프레임에 실행한다.
- 2026-07-22: 이미지 투명화 도구를 실행하며 이 환경에 `python` 명령이 있을 것이라 가정해 첫 후처리가 실패했다. 저장소 밖 보조 스크립트를 실행하기 전 `command -v python3` 등 실제 인터프리터를 확인하고 이 환경에서는 `python3`를 우선 사용한다.
- 2026-07-22: 자원 HUD의 0개 숨김 테스트가 앞선 Money 테스트에서 남긴 수정·석유·우라늄을 초기화하지 않아 정상 표시를 실패로 판정했다. 여러 자원 딕셔너리를 공유하는 테스트는 필요한 키만 덮지 말고 모든 키를 기준 상태로 초기화하거나 별도 Main 인스턴스를 사용한다.
- 2026-07-22: Web 저장 복원 검증에서 모바일 자동화 탭이 개발 버튼 상태를 바꾸지 못한 것을 IndexedDB 저장 실패로 성급히 판단했다. 영구 저장 검증은 저장 전 상태 변화가 화면에서 확인됐는지 먼저 assertion하고, `page.reload()`의 Godot 프레임 detach 대신 같은 브라우저 컨텍스트의 새 페이지에서 복원 상태를 비교한다.
- 2026-07-22: 기지 메뉴의 X 의미를 선택에서 나가기로 바꾸면서 `test_mining.gd`에 남은 기존 X 선택 시나리오를 사전 검색에서 놓쳐 전체 테스트가 중간 실패했다. 입력 의미를 변경할 때는 UI 문구와 입력 코드뿐 아니라 해당 액션 함수 호출 및 과거 동작을 설명하는 assertion 문자열까지 저장소 전체에서 검색해 함께 갱신한다.
- 2026-07-22: 자연 진행 중인 하루 시계를 모바일 `+1분` 테스트에서 정확히 60.0초 차이로 비교해, 테스트 프레임의 실제 delta가 추가되면서 정상 기능을 실패로 판정했다. 실행 중인 시간·물리 누적값은 정지시킨 단위 테스트가 아니라면 기대 증가량 이상이면서 한 프레임 오차 이내인지 범위로 검증한다.
- 2026-07-22: 모바일 터치 분기에서 `if event.pressed`를 중첩해 릴리스가 `_end_touch()`에 도달하지 못하는 구조를 뒤늦게 발견했다. 터치 자동 테스트는 내부 종료 함수를 직접 호출하지 말고 실제 `InputEventScreenTouch`의 press/release 쌍을 `_input()`에 전달해 조이스틱과 버튼 상태가 해제되는지 검증한다.
- 2026-07-22: 새 연구 메뉴의 반복문 좌표를 타입 없이 선언해 기존에 여러 번 기록된 GDScript 수치 타입 추론 오류를 다시 만들었다. UI 반복 계산의 실수 좌표도 처음부터 `float`로 명시하고 headless 출력의 `SCRIPT ERROR`를 검사한다.
- 2026-07-22: 동결 카운트다운 Web 캡처용 Playwright 스크립트의 viewport 객체 닫는 중괄호를 또 빠뜨려 실행 전에 문법 오류가 났다. 일회성 브라우저 스크립트도 실행 전 객체 리터럴의 괄호 쌍을 검토하고, 문법 실패를 기능 실패와 구분한다.
- 2026-07-22: 기지 2단계에 미네랄 3개를 보장하는 로직에서 `base_level - 1`개만 추가해 기존 1개와 합계 2개가 됐다. 단계별 필수 구성은 추가량이 아니라 최종 총량을 자동 테스트하고, 기존 개수를 센 뒤 목표 총량까지 채운다.
- 2026-07-22: 터치 회귀 테스트가 assertion 실패 시 `quit(1)`을 예약한 뒤 마지막 `quit(0)`과 PASS 출력으로 실패를 덮는 과거 패턴을 반복하고 있었다. 모든 SceneTree 테스트는 실패 수를 누적하고 마지막에 단 한 번 `quit(failures)`를 호출한다.
- 2026-07-22: 모바일 뷰포트 브라우저에서 개발 버튼을 검증하면서 `mouse.click()`을 사용해 실제 `InputEventScreenTouch`가 전역 조작 레이어에 소비되는 문제를 놓쳤다. 모바일 터치 기능은 반드시 `touchscreen.tap()` 또는 실제 touch/drag 이벤트로 연속 입력까지 검증한다.
- 2026-07-22: JavaScript 템플릿 문자열로 `apply_patch` 입력을 만들면서 문서에 넣을 백틱을 이스케이프하지 않아 패치 실행 전 문법 오류가 났다. 패치 본문에 백틱이 포함되면 일반 따옴표 문자열을 쓰거나 백틱을 명시적으로 이스케이프한다.
- 2026-07-21: 여러 파일을 한 패치로 수정하다 뒤쪽 파일의 문맥 불일치로 실패했는데 앞쪽 파일 변경은 이미 반영돼 있었다. 다중 파일 패치가 실패하면 전체가 원복됐다고 가정하지 말고 `git diff`와 대상 파일을 확인한 뒤 중복 없이 재시도한다.
- 2026-07-21: `Vector2.normalized()` 호출 결과를 GDScript `const`에 넣어 기지 숙소 방향 스크립트가 파싱되지 않았다. `const`에는 컴파일 타임 상수식만 사용하고 정규화 방향은 계산된 고정 성분을 쓰거나 런타임 변수로 선언한다.
- 2026-07-21: 튜토리얼 완료 여부를 즉석 bool 배열에서 꺼내며 타입을 생략해, 같은 날 기록한 GDScript 동적 배열 추론 오류를 반복했다. 배열 리터럴을 인덱싱하는 모든 신규 코드는 커밋 전 `Array[구체타입]` 선언 여부를 별도 검색·검토한다.
- 2026-07-21: 시작 구역 배치용 `Vector2i` 배열과 반복 변수를 비정형으로 선언해 Godot이 합산 결과 타입을 추론하지 못했다. 좌표·방향 컬렉션은 `Array[Vector2i]`처럼 원소 타입을 선언하고 반복 변수와 계산 결과에도 명시적 타입을 사용한다.
- 2026-07-20: GitHub Pages Web export 배포를 HeyDive 게임 목록 등록과 같은 것으로 간주했다. `gamo`의 push는 정적 파일만 배포하며, 현재 HeyDive 목록은 별도 서버 DB에서 관리된다. 이후 배포 완료를 보고할 때 정적 URL 응답과 HeyDive API 등록 여부를 각각 확인한다.
- 2026-07-20: Godot 기본 폰트가 Web export에서도 한글을 표시할 것이라 가정해 UI 한글이 깨졌다. 한글 UI가 있는 게임은 OFL 등 배포 가능한 한글 폰트를 프로젝트에 포함하고 모든 관련 Control에 명시적으로 적용한 뒤 Web 빌드에서 확인한다.
- 2026-07-20: HeyDive 서버 코드를 push한 뒤 실행 중인 Docker 서버도 자동 배포될 것이라 가정해 Motorio가 실제 목록에 노출되지 않았다. HeyDive 게임 등록 변경은 `docker compose up -d --build server`로 서버를 갱신하고 localhost와 `api.heydive.in`의 `/api/games` 응답을 모두 확인한다.
- 2026-07-20: 폰트 서브셋 작업 전에 `pip`/`ensurepip` 사용 가능 여부를 확인하지 않아 설치 명령이 두 번 실패했다. 변환 도구가 필요하면 먼저 `command -v`와 모듈 가용성을 확인하고, 없다면 시스템을 변경하지 않는 저장소 내 대안을 우선한다.
- 2026-07-20: 탑다운 게임의 `RigidBody2D` 갈색 타일에 기본 중력이 적용되어 아래로 계속 움직였다. 평면 탑뷰 물체는 `gravity_scale = 0`을 명시하고, 정지 요구가 있으면 감쇠와 저속 임계 정지를 함께 검증한다.
- 2026-07-20: 불필요해진 폰트 파일을 `rm -f`로 삭제하려다 안전 정책에 의해 명령이 거부됐다. 이미 추적 중인 파일은 패치로 삭제하고, 큰 바이너리는 정확한 단일 경로를 확인한 뒤 `unlink`를 사용한다.
- 2026-07-20: 걷기 모션의 삼항 조건식에서 GDScript가 `bob`의 타입을 추론하지 못해 파싱 오류가 발생했다. 수치 애니메이션 지역 변수에는 `float` 타입을 명시한다. 또한 Godot headless가 스크립트 오류에도 종료 코드 0을 반환할 수 있으므로 명령 성공 여부뿐 아니라 출력의 `SCRIPT ERROR`/`ERROR`도 확인한다.
- 2026-07-20: GitHub Pages의 약 10분 브라우저 캐시 때문에 같은 URL에서 이전 Web 빌드가 계속 보였다. HTTP 캐시 헤더는 저장소에서 제어할 수 없으므로 배포 시 내용 해시 파일명과 cache-busting 버전 로더를 사용한다.
- 2026-07-20: 컨베이어를 고정 `Area2D`로 구현해 플레이어와 겹칠 수 있었고 갈색 상자처럼 밀리지 않았다. 물리 블록 요구가 있으면 고체 충돌/중력/감쇠/밀기 동작과 효과 감지 영역을 별도로 설계해 모두 확인한다.
- 2026-07-20: GitHub Pages의 `version.json`에 시간 쿼리를 붙이면 캐시가 분리될 것으로 예상했지만 Pages CDN은 쿼리를 무시하고 동일 캐시를 최대 10분 제공했다. Pages의 최신 버전 판단에는 GitHub Contents API를 사용하고, 실제 공개 경로와 HeyDive iframe 양쪽에서 검증한다.
- 2026-07-20: 저장소 API로 최신 해시를 즉시 알아도 GitHub Pages 배포 전에 새 해시 URL을 조회해 404가 캐시됐다. 업데이트된 PCK는 Pages 준비 여부를 미리 조회하지 말고, 고정 runner가 저장소의 고유 해시 PCK를 직접 다운로드하게 한다.
- 2026-07-20: 모바일 컨트롤 테스트에서 배열 원소와 동적 씬 인스턴스의 GDScript 타입 추론 오류가 발생했다. 상수 배열에서 꺼내는 값과 `load().instantiate()` 결과는 명시적으로 타입을 지정하고, 정적 `Node`의 사용자 정의 속성은 `get()`으로 검증한다.
- 2026-07-20: cache-busting runner에서 `Engine.start()` 전에 `Engine.init(executable)`을 호출하지 않아 브라우저에서 “A base path must be provided” 오류가 발생했다. Web runner는 엔진 초기화 → PCK preload → start 순서를 지키고, Node 문법 검사뿐 아니라 실제 브라우저 실행 테스트를 필수로 한다.
- 2026-07-20: `npx -p playwright node -e`가 임시 Playwright 모듈을 Node 검색 경로에 제공할 것으로 가정해 브라우저 테스트 실행이 실패했다. 일회성 브라우저 테스트는 명시적 임시 prefix에 패키지를 설치하고 해당 `node_modules`를 `NODE_PATH`로 지정한다.
- 2026-07-20: 컨베이어 화살표 wrap 계산이 32×32 블록 범위를 벗어났고, 효과 감지 영역을 충돌체보다 크게 만들어 외곽선 접촉만으로 이동이 적용됐다. 애니메이션 점의 최대 범위와 엄격한 내부 판정을 각각 자동 테스트한다.
- 2026-07-21: 탑뷰 캐릭터에 `CharacterBody2D` 기본 `GROUNDED` 모드를 사용해 아래쪽의 움직이는 상자를 바닥 플랫폼처럼 인식하고 속도를 전달받았다. 중력 없는 평면 이동 캐릭터는 `MOTION_MODE_FLOATING`을 명시하고 방향별 밀기 동작을 검증한다.
- 2026-07-21: SceneTree 초기화 테스트에서 `@onready` 변수가 준비됐다고 가정해 애니메이션 테스트가 Nil 오류를 냈고, 개별 assertion에서 `quit(1)`을 예약한 뒤 마지막에 `quit()`으로 성공 코드를 덮었다. 초기화 전 직접 호출 테스트는 노드 의존성을 명시적으로 연결하고, 실패 수를 모아 마지막에 한 번만 종료 코드를 결정한다.
- 2026-07-21: 생성형 이미지의 스프라이트 시트가 각 셀 안에서 같은 중심에 정렬됐다고 가정해 대기 중 캐릭터 전체가 좌우로 흔들렸다. 스프라이트 시트 적용 전 각 프레임의 알파 경계와 발 기준점을 측정하고 공통 기준점으로 보정한 뒤 연속 프레임을 검증한다.
- 2026-07-21: 발 기준점 수치만 정렬한 대기 프레임 테스트를 통과시킨 뒤 모바일에서 실루엣 자체가 좌우로 바뀌는 문제를 놓쳤다. 대기 애니메이션은 기준점뿐 아니라 몸통/머리의 연속성도 모바일 해상도의 실제 연속 캡처로 확인하며, 생성 프레임의 구도가 불연속이면 단일 등록 프레임 기반 절차적 모션을 사용한다.
- 2026-07-21: 대기 흔들림만 안정화하고 동일한 불연속 생성 프레임을 사용하는 걷기/달리기 상태를 함께 수정하지 않아 이동 중 좌우 흔들림이 남았다. 애니메이션 정렬 결함을 발견하면 모든 상태를 한 번에 감사하고, 모바일에서 정지·걷기·달리기 각각 연속 프레임 검증을 수행한다.
- 2026-07-21: 걷기/달리기 절차적 모션을 추가하며 과거에 기록한 GDScript 조건식 수치 타입 추론 문제를 다시 만들었다. 애니메이션 계산의 `rate`, `phase`, `stretch`, `squash`, `bounce` 같은 실수 변수는 작성 시 모두 `float`를 명시하고 편집기 로드를 첫 검증으로 실행한다.
- 2026-07-21: 모바일 WebGL 한 세션에서 고해상도 연속 캡처를 너무 많이 수행해 Chromium GPU 프로세스가 중간에 멈췄다. 모바일 애니메이션 검증은 상태별 브라우저 세션을 분리하고 10~12프레임 단위로 캡처해 테스트 도구 정지를 피한다.
- 2026-07-21: 걷기 흔들림을 줄이는 과정에서 모션 강도를 너무 작게 만들어 모바일에서는 정지 이미지처럼 보였고, 좌우 반전된 스프라이트의 발 기준점 X를 반전하지 않아 왼쪽 전환 순간 위치가 튀었다. 애니메이션은 수치 안정성뿐 아니라 모바일에서 시각적 가독성을 확인하고, `flip_h` 보정은 렌더 변환과 기준점 계산에 동시에 적용한다.
- 2026-07-21: 런타임에 코드로 생성한 거점 투입구를 테스트하며 `find_children()`의 기본 `owned = true` 때문에 owner가 없는 노드를 찾지 못했다. 동적 노드 검색 테스트는 네 번째 인자를 `false`로 지정하거나 직접 자식 목록을 검사한다.
- 2026-07-21: 공개 Web 검증에서 index의 runner 리다이렉트를 기다리지 않아 `canvas: 0`을 얻었고, 같은 셸 명령 뒤의 `git status`가 브라우저 테스트 실패 종료 코드를 덮었다. 공개 검증은 runner URL 전환과 status 제거를 모두 기다리고, 검증 프로세스는 별도 명령으로 실행해 종료 코드를 보존한다.
- 2026-07-21: 인벤토리 UI에서 동적 컨트롤러 속성과 비교한 `selected` 값을 GDScript가 추론할 것으로 두어 또 파싱 오류가 발생했고, 테스트에서 typed `Array[Dictionary]`에 untyped 배열을 통째로 대입했다. 동적 객체를 참조하는 UI 지역 변수는 기본형도 타입을 명시하고, typed 컬렉션 테스트 데이터는 `clear()` 후 원소별로 추가한다.
- 2026-07-21: 충돌 마스크만 확인하려고 씬을 instantiate한 테스트 노드를 트리에 넣지도 해제하지도 않아 종료 시 물리 RID 누수 경고가 발생했다. 트리 밖 임시 노드는 assertion 직후 `free()`하고, 테스트 출력의 leak 경고도 실패와 동일하게 처리한다.
- 2026-07-21: 미니맵이 매 프레임 1,000개 이상의 그룹 노드를 다시 검색하고 개별 점을 그리게 구현해 모바일 렌더링 부하를 만들었다. 미니맵·통계 UI는 프레임 단위 갱신을 피하고, 저주기 캐시와 공간 버킷 집계로 표시 밀도를 제한한다.
- 2026-07-21: Playwright 모듈이 있는 임시 디렉터리에서 로컬 Web 검증을 실행하면서 저장소 manifest를 상대 경로로 읽어 pack 이름이 비었는데도 기본 pack으로 게임이 실행됐다. 배포 검증은 manifest 절대 경로를 사용하고, 실행 URL에 기대한 해시 pack이 포함됐는지 assertion한다.
- 2026-07-21: 기존 InputMap의 좌·우 화살표 숫자가 실제로는 Home/Page Up 키였는데 화살표 코드라고 가정했다. 특수 키 매핑은 숫자를 추측하지 말고 `KEY_LEFT` 같은 Godot 상수와 `InputMap.action_get_events()`를 비교하는 자동 테스트로 검증한다.
- 2026-07-21: 인벤토리 테스트에서 `queue_free()`로 해제된 원본 블록의 스크립트를 이후 미리보기 비교에 다시 참조해 테스트가 중단됐다. 해제 예정 인스턴스의 속성이 필요하면 미리 저장하거나 리소스 경로를 직접 비교한다.
- 2026-07-21: 모드 불빛 위치 테스트에서 플레이어 방향을 LEFT로 바꾼 뒤 복구하지 않아 뒤따르는 대기 애니메이션 검증의 좌우 반전 상태를 오염시켰다. 하나의 인스턴스로 여러 상태를 검사하는 테스트는 각 구간 뒤 기준 상태를 명시적으로 복원한다.
- 2026-07-21: 키보드·마우스 감지 시 터치 UI를 숨기면서 모바일 브라우저가 터치 직후 생성하는 합성 마우스 이벤트를 구분하지 않아 조이스틱 상태가 즉시 초기화됐다. Web 터치의 마우스 에뮬레이션을 끄고, 터치 직후 짧은 시간의 마우스 이벤트를 무시하며 실제 브라우저 드래그로 검증한다.
- 2026-07-21: 시작 안전 구역을 지역 크기보다 크게 확장하면서 무작위 빈 칸 탐색을 무한 `while`로 유지해 중앙 지역에서 월드 생성이 멈췄다. 절차 생성의 무작위 탐색은 항상 시도 횟수 상한과 실패 반환값을 두고, 빈 후보가 없는 구역을 건너뛴다.
- 2026-07-21: 동적 그룹 노드의 좌표와 상수 방향을 더한 지역 변수 타입을 추론에 맡겨 `Base.gd` 파싱이 실패했다. 동적 객체 속성이 포함된 벡터 계산은 결과 변수에 `Vector2`를 명시하고 editor load로 먼저 검증한다.
- 2026-07-21: 새 Web export를 로컬에서 확인하면서 안정 `index.html`을 열어 GitHub API가 가리키는 이전 원격 PCK를 검증했다. 푸시 전 로컬 검증은 새 해시의 `game-*.html`을 직접 열고, 푸시 후 공개 검증은 runner 요청 URL의 기대 PCK 해시를 assertion한다.
- 2026-07-21: 기지 표기를 한글화하면서 반복문 안에서 쓰는 폰트 변수의 들여쓰기를 반복문 밖으로 잘못 옮겨 `Base.gd` 파싱이 실패했다. 코드 치환 뒤에는 주변 블록의 들여쓰기를 확인하고 editor load 출력에서 `SCRIPT ERROR`까지 검사한다.
- 2026-07-21: 물 타일에 눈과 구분되는 청록색을 지정했지만 음수 `z_index` 때문에 부모 지형 뒤에 그려져 실제 게임에서는 보이지 않았다. 바닥형 오브젝트도 부모 `_draw()`와의 표시 순서를 실제 화면에서 확인하고, 지형 위 렌더 순서를 자동 테스트한다.
- 2026-07-21: 한글 폰트의 Godot `.import` 파일을 불필요한 로컬 생성물로 판단해 삭제하자 다음 첫 실행에서 테마가 폰트 재가져오기보다 먼저 로드되어 오류가 났다. 원본 자산 옆의 `.import`는 재현 가능한 가져오기 설정이므로 함께 커밋하고, `.godot/` 내부 캐시만 제외한다.
- 2026-07-21: 물길 실제 이동 캡처용 Playwright 한 줄 스크립트에서 viewport 객체의 닫는 중괄호를 빠뜨려 브라우저가 실행되지 않았다. 복잡한 일회성 검증 스크립트도 실행 전 괄호 구조를 확인하고, 실패 종료 코드를 게임 검증 성공으로 오인하지 않는다.
- 2026-07-31: 브라우저 FPS를 한 번만 재고 성능 결론을 내려 정반대로 판단했다. 같은 빌드가 5.1~7.8 fps로 흔들려, 개선된 변경을 "더 느려졌다"고 오판하고 엉뚱한 원인(바이리니어 필터)까지 지목했다. 렌더링 성능 비교는 반드시 같은 빌드를 3회 이상 재고 중앙값으로 판단하며, 단조롭지 않은 결과가 나오면 결론 대신 노이즈를 의심한다.
- 2026-07-31: SceneTree 테스트에서 런타임 타입 오류가 나자 `_run`이 중단되어 `quit()`에 도달하지 못했고, 테스트가 실패 대신 무한 대기했다. 원인은 로그 맨 끝에만 있었고 출력이 없어 진단이 늦었다. 테스트가 결과 없이 멈추면 타임아웃을 늘리지 말고 `timeout -s KILL`로 로그를 파일에 받아 마지막 `SCRIPT ERROR`를 먼저 확인한다.
- 2026-07-31: `Main._ready()`가 저장 파일을 자동으로 불러오기 때문에, 앞서 실행된 `test_save`가 남긴 세이브를 `test_build`가 물려받아 이미 설비가 있는 월드에서 시작했다. Main 씬을 인스턴스화하는 테스트가 깨끗한 월드를 전제한다면 `clear_save()`와 `_start_run()`을 명시적으로 호출한다.
- 2026-07-31: 체온 0 처리에서 `_update_warmth`가 매 프레임 낙하를 진행한 뒤 체온을 다시 검사하는데, 낙하 중에는 유예 타이머가 음수라 `collapse_timer >= 0.0` 가드를 통과해 유예가 5초로 재무장됐다. 결과는 5초마다 한 프레임씩만 쓰러지는 무한 루프였다. 상태 진입 가드는 타이머 하나가 아니라 그 상태를 나타내는 모든 변수(진행도·연출 단계)를 함께 검사하고, 단위 테스트는 내부 함수를 직접 부르지 말고 실제 매 프레임 경로 전체를 돌린다.
- 2026-08-02: `motorio-oneshot`의 밤 어두워짐은 월드 레이어가 아니라 HUD의 `_draw_cold_vignette`가 `day_fraction()`을 직접 읽어 화면 전체에 덧칠한다. 밤 연출용 상태를 새로 추가하면서 HUD `_draw()`에 자체 분기를 두자, 밤인데 화면이 대낮으로 밝아졌다. 월드를 보여주는 상태를 추가할 때는 HUD의 화면 전체 오버레이를 함께 그리는지 확인하고, 시간 관련 표시는 시계가 아니라 `night_level()`처럼 연출이 덮어쓸 수 있는 단일 함수를 읽게 한다.
- 2026-08-02: 연출(컷신)의 각 구간은 조건뿐 아니라 반드시 시간 상한으로도 진행시킨다. 숙소로 걸어오지 못하는 고양이 한 마리가 밤을 영원히 붙잡을 수 있고, 끝나지 않는 연출은 연출이 아니라 멈춤이다. 테스트는 "도달할 수 없는 대상"을 실제로 만들어 상한 안에 끝나는지 확인한다.
- 2026-08-02: `test_tiles`가 무작위 지형 시드 때문에 10번에 1번쯤 실패했다(사방이 트인 광맥이 없는 맵). 절차 생성 월드를 전제로 조건을 찾는 테스트는 고정 시드로 `sim.setup(seed)`를 호출한다. 가끔 실패하는 테스트는 없는 테스트보다 나쁘다 — 재실행하는 습관을 가르친다.
- 2026-08-06: 빌드 산출물을 정리하는 셸 한 줄에서 `cd web && npm run build && ... docs/site-assets/...`처럼 상대 경로를 이어 붙여, 정리 대상이 `web/docs/site-assets`로 해석되어 아무것도 지워지지 않았다. 옛 번들이 남아 옛 에셋 이름을 계속 가리켰다. 여러 디렉터리를 오가는 명령의 파일 경로는 저장소 루트 기준 절대 경로로 쓰고, 정리 후 실제 파일 수를 세어 확인한다. 어떤 산출물이 죽었는지는 이름으로 짐작하지 말고 `docs`의 html·js·css 전체에서 참조 0건임을 확인한 뒤 지운다.
- 2026-08-07: nginx의 `types { }` 블록은 상속받은 MIME 맵을 확장하는 게 아니라 **통째로 교체**한다. wasm 하나를 선언하려다 나머지 전부를 잃어 `index.html`이 `application/octet-stream`으로 나갔고, 브라우저는 렌더 대신 다운로드한다. nginx 1.27의 기본 `mime.types`에 이미 `application/wasm`이 있으니 블록 자체가 불필요했다. 헤더는 짐작하지 말고 배포 직후 `curl -sI`로 대표 확장자마다 확인한다.
- 2026-08-07: **단일 파일**을 bind mount하면 그 파일을 편집할 때 inode가 바뀌어 컨테이너는 옛 파일을 계속 본다. `nginx -s reload`는 낡은 파일을 다시 읽고 성공을 보고하므로, 맞는 수정을 틀렸다고 오판하게 만든다. 설정 파일을 고쳤으면 reload가 아니라 컨테이너를 재생성한다.
- 2026-08-07: Docker는 없는 bind mount 경로를 **root 소유로** 만든다. 일반 사용자로 도는 배포 스크립트가 거기에 쓸 수 없다. 마운트 경로는 컨테이너를 띄우기 전에 만들고, 이미 root 소유가 됐고 sudo가 없다면 `docker run --rm -v <경로>:/x alpine chown -R $(id -u):$(id -g) /x`로 되돌린다.
- 2026-08-06: 영상 생성 레퍼런스 이미지의 배경은 결과물의 배경을 결정한다. 주인공 레퍼런스는 순수 초록(0,255,0) RGB였는데 고양이 레퍼런스를 투명(RGBA)으로 만들었더니 모델이 탁한 회녹색 배경을 지어냈고, 크로마키가 걷어내지 못해 캐릭터 뒤에 배경 덩어리가 프레임마다 흔들렸다. 새 레퍼런스는 항상 기존 레퍼런스와 같은 모드·같은 배경색으로 만들고, 생성 직후 원본 프레임의 네 모서리 픽셀값을 찍어 기존 클립과 비교한다. 다시 생성하는 비용은 레퍼런스를 확인하는 비용보다 훨씬 크다.
- 2026-08-06: 검증 허용치를 절대 픽셀로 적어두면 셀 크기를 바꾸는 순간 조용히 의미가 바뀐다. `max_centroid_jump_px = 3.0`은 64 셀에서 정한 값인데 128 셀을 추가하자 같은 동작이 두 배로 재어져 멀쩡한 시퀀스가 거부됐다. 크기에 비례하는 값은 몸통 높이에 대한 비율로 적는다. 반대로 발 기준점 오차(0.5px)처럼 격자 양자화에서 오는 값은 절대 픽셀이 맞다 — 어떤 종류의 값인지 먼저 가른다.
- 2026-08-06: "시퀀스 자신의 중앙값 대비 몇 배"라는 상대 규칙은 매 프레임에 똑같이 나타나는 결함을 못 잡는다. 배경 덩어리가 모든 프레임에서 흔들린 클립은 점프가 고르게 커서 중앙값 대비 1.6배였고, 이는 정상 달리기의 2.3배보다 낮았다. 시퀀스를 자기 자신과 비교하는 규칙은 전역 결함에 눈이 먼다.
- 2026-08-06: 좌표 규약을 축마다 다르게 쓰면 반전에서 드러난다. `_foot`의 y는 픽셀 경계(bottom+1)인데 x는 픽셀 인덱스 평균이라, 폭 128 셀의 기준점 x=64는 실제 중심 63.5보다 반 픽셀 오른쪽이었다. 셀 중심으로 반전하니 발이 1px 밀렸다. 픽셀 좌표는 인덱스와 경계 중 하나로 통일하고(여기서는 x+0.5로 경계 기준), 반전 결과를 실제로 측정해 확인한다.
- 2026-08-06: 생성 프롬프트에서 동작은 "무엇을 한다"가 아니라 "어디에서 어디까지 움직인다 + 몇 번 반복"으로 적는다. "곡괭이를 위아래로 휘두른다"는 4초 내내 곡괭이를 든 채 자세만 바꾸는 클립을 만들었고 찾을 사이클이 없었다. 머리 위에서 지면까지의 이동과 반복 횟수를 명시하자 해결됐다. 반복 횟수는 클립 안에 사이클이 들어가게 만드는 장치이기도 하다.
- 2026-08-06: `cd web && npm run build` 뒤에 `docs/...`를 이어 쓰는 실수를 같은 날 두 번 했다. 한 번은 정리가 조용히 아무것도 안 지웠고, 두 번째는 교훈을 AGENTS에 적은 직후에 반복했다. 규칙을 적는 것으로는 안 고쳐진다 — 디렉터리를 바꾸는 명령 뒤의 경로는 예외 없이 저장소 루트 절대 경로 변수(`G=/home/merti/Workspace/gamo`)로 쓴다.
- 2026-08-06: 빌드 산출물 정리는 한 번으로 끝나지 않는다. 옛 진입점 번들을 지우면 그것만 참조하던 청크가 그때서야 미참조가 되므로, 더 이상 줄지 않을 때까지 반복하고 마지막에 모든 참조가 실제 파일로 해결되는지 확인한다.
- 2026-08-06: "고쳤는데 화면이 그대로다"는 세 가지 서로 다른 원인이 같은 모습으로 나타난다 — (1) 브라우저/CDN 캐시, (2) Pages가 아직 그 커밋을 배포하지 않음, (3) 수정 자체가 틀림. 순서대로 갈라야 한다. 배포된 HTML이 참조하는 해시 번들 이름을 로컬 빌드 결과와 비교하면 (2)가 즉시 드러나고, 로컬 산출물을 직접 측정하면 (3)이 드러난다. 실제로 Pages가 50분간 두 커밋을 건너뛴 채 이미 저장소에서 지워진 옛 파일을 200으로 계속 서빙했고, 그동안 새 해시 파일은 404였다. 사용자를 기다리게 하지 말고 로컬 산출물을 직접 보여준다.
- 2026-08-06: 정규화된 스프라이트의 실루엣 높이가 8프레임 모두 정확히 같다고 해서 프레임별 스케일 버그가 남았다는 뜻은 아니다. 원본 편차가 1.3%면 32px로 줄일 때 반올림으로 사라진다. 지표가 이상하리만치 깨끗하면 원본 편차와 목표 해상도를 함께 계산해 그 값이 나올 수 있는지 확인하고, 자기 코드를 의심하기 전에 산수를 먼저 한다.
- 2026-08-06: GitHub Pages에 올리는 **모든** 생성물의 파일명에 내용 해시를 붙인다. 게임 PCK에는 이미 적용했는데 스프라이트 후보 시트에는 빼먹었고, 같은 이름으로 수정본을 올리자 `max-age=600` 때문에 페이지가 계속 옛 그림을 보여줬다. 사용자는 "수정이 안 됐다"고 보고했고 그게 맞았다 — 서버가 옛것을 주고 있었으니까. 내용이 바뀌면 URL이 바뀌어야 한다. 반대로, 수정 직후 공개 URL을 검증할 때는 Pages 반영 지연(수 분)과 캐시를 구분해서 `last-modified`·`age` 헤더를 함께 확인한다.
- 2026-08-06: 스케일된 Control 위의 텍스트는 `oversampling`이 아니라 **MSDF**로 해결한다. 글리프가 설계 크기로 래스터화된 뒤 캔버스 배율로 축소되면 1px 획이 사라지고, 한글에서는 「바라보고」가 「비리보고」가 된다(ㅏ의 가로획 소실). oversampling 2배는 큰 글자만 개선하고 작은 글자는 그대로였다. 그리고 이건 "흐림"이 아니라 "획 소실"이므로, 원인을 찾을 때 폰트 파일 자체를 PIL 등으로 직접 렌더해 파일과 렌더러 중 어느 쪽 문제인지부터 가른다.
- 2026-08-06: 폰트 `.import`의 `hinting`은 0~2, `subpixel_positioning`은 0~3이다. 이 저장소에는 각각 3과 4가 들어 있었다. enum·keycode 숫자는 추측하지 말고 `TextServer.HINTING_*`처럼 상수를 출력해 확인한다(같은 교훈이 keycode에서도 이미 있었다).
- 2026-08-06: 한글 UI가 전부 뭉개져 「저장할」이 「서상알」로 보였다. HUD가 `Control.scale`로 축소돼 글리프가 설계 크기로 래스터화된 뒤 리샘플링되고 있었고, 작은 획이 그 과정에서 사라졌다. 폰트 `.import`의 `oversampling`을 2.0으로 올려 해결. 스케일된 Control 위에 텍스트를 그리는 UI는 이 설정을 반드시 확인한다 — 읽히기는 해서 버그로 인지되지 않고 "UI가 그냥 별로다"로만 남는다.
- 2026-08-06: 화면 전체 오버레이 셰이더의 비용은 연산이 아니라 픽셀을 채우는 것이다. 픽셀당 `sin` 기반 그레인을 넣은 버전과 뺀 버전의 fps가 같았다(소프트웨어 래스터라이저 기준). 따라서 효과는 "싸게" 설계할 게 아니라 "그 채우기 비용을 낼 값어치가 있게" 설계한다. 그리고 WebGL에서 `SCREEN_TEXTURE` 읽기는 매 프레임 프레임버퍼 복사이므로 블룸류는 이 예산에서 불가능하다.
- 2026-08-06: 절차 생성 월드에서 "이 칸은 항상 비어 있어야 한다"는 조건은 한 시드로 검사하면 안 된다. 숙소 문 앞에 광맥이 생기는 버그가 형제 테스트에서 5번에 1번 실패로 나타났다 — 시드가 매번 달라 대부분의 맵은 멀쩡하기 때문이다. 예약 칸은 모든 scatter가 끝난 뒤 마지막에 지우고, 테스트는 수백 개 시드를 훑는다.
- 2026-08-03: "들려 있는 고양이는 시뮬레이션하지 않는다" 검사를 9개 핸들러 중 3곳에만 넣어, 나머지 6곳에서 들린 고양이가 계속 걸어가고 스스로 상태를 바꾸고 팔에 안긴 채로 채굴기를 돌렸다. `carry_at`과 `_step_toward`가 같은 프레임에 위치를 써서 스프라이트가 손과 목적지 사이를 튀었다. 예외 조건은 각 분기가 아니라 분기가 갈라지기 전 한 곳에서 검사한다 — 분기마다 넣는 방식이 바로 누락을 만든다.
- 2026-08-03: 도착 판정에서 `pos = goal`로 스냅하면 남은 거리만큼 한 프레임에 순간이동한다. 고양이는 10px(0.2초 거리)를 튀었다. 이동체의 "도착"은 위치를 목표에 맞추는 것이 아니라 반경 안에 들어온 것으로 판정하고, 한 틱 이동량은 항상 속도×delta를 넘지 않게 한다. 테스트로 최대 한 틱 이동량을 고정한다.
- 2026-08-03: 벨트가 초반에 한 칸도 움직이지 않는 버그를 기존 테스트 전부가 놓쳤다. 원인은 모든 벨트 테스트가 `_power(sim)` 헬퍼를 먼저 호출해 발전기를 켜둔 세계에서 실행됐다는 것이다. 테스트의 셋업 헬퍼가 검사하려는 문제를 미리 해결해 주면, 실제 플레이어가 겪는 상태는 한 번도 실행되지 않는다. 기능 테스트를 쓸 때 셋업이 무엇을 보장하는지 적어두고, 그 보장이 없는 경로(해금 전·전력 전·자원 0)를 별도 파일에서 명시적으로 금지 규칙과 함께 검사한다.
- 2026-08-03: 전력 소비를 붙일 때는 그 설비가 발전기보다 먼저 해금되는지 확인한다. 벨트(첫 구리)에 소비를 붙였더니 발전기가 없는 동안 `capacity / draw = 0 / 0.1 = 0`이 되어 속도가 0이 됐다. 비율로 감속시키는 설계에서 분자가 0이 될 수 있으면 감속이 아니라 정지다.
- 2026-08-03: 모바일 첫 접속이 몇 분 걸리는 원인이 `NotoSansCJK-Regular.ttc` 19MB였다. Godot은 폰트를 통째로 export에 넣으므로 PCK 18.2MB 중 16.3MB(90%)가 이 게임이 한 번도 그리지 않는 일본어·중국어 글리프였다. 한글 UI 게임에는 전체 CJK 폰트를 넣지 말고 소스에서 문자 집합을 추출해 서브셋한다(`motorio-oneshot/tools/build_font.cjs`, 19MB→90KB). 원본은 export에서 제외되는 `tools/`에 두고 저장소에는 남긴다. 손으로 문자 목록을 적으면 반드시 낡으므로, 소스의 모든 비ASCII 문자가 폰트에 있는지 검사하는 테스트(`test_font.gd`)를 함께 둔다.
- Web 용량은 추측하지 말고 실제로 잰다. `ls -la docs/<게임>/`로 파일 크기, `curl -sI -H 'Accept-Encoding: gzip'`로 전송 크기와 `content-encoding`, Playwright `requestfinished` + `request.sizes()`로 요청별 시각과 바이트를 확인한다. Godot Web의 엔진 wasm 약 10MB(gzip)는 템플릿 고정값이라 저장소에서 줄일 수 없고, ETag 재검증으로 재방문 시 0바이트다 — 줄일 수 있는 것은 PCK뿐이다.
- 게임 문서는 게임별로 `web/app/{게임}/doc/`에 독립적으로 둔다. `/gamo/doc/`은 저장소 수준 문서와 레퍼런스만 담고, 각 게임 문서로 링크만 한다. 문서 레이아웃은 `web/components/DocShell.jsx`를 공유한다.
- `motorio-oneshot`의 밸런스 수치는 문서에 손으로 적지 않는다. `godot --headless --path motorio-oneshot --script res://tools/dump_balance.gd`로 `web/lib/generated/balance.json`을 생성하고 문서 페이지가 그것을 읽는다. 수치를 바꾸면 같은 커밋에서 반드시 다시 생성한다. 손으로 적은 수치는 반드시 낡는다 — 벨트 속도가 10배 바뀌었는데 레벨 디자인 페이지가 세 버전 동안 옛 값을 말하고 있었다.

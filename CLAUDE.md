# Gamo 작업 규칙 (Claude Code 진입점)

이 저장소의 공동 작업 규칙은 `AGENTS.md` 하나로 관리한다. Codex는 `AGENTS.md`를,
Claude Code는 이 파일을 자동으로 읽으므로, 아래 import로 동일한 내용을 공유한다.

규칙을 추가하거나 수정할 때는 **항상 `AGENTS.md`만 편집한다.** 이 파일에는 규칙 본문을
복사해 두지 않는다. 두 곳에 나뉘면 도구마다 다른 규칙을 따르게 된다.

@AGENTS.md
